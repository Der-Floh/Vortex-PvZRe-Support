on:
  release:
    types: [published]

permissions:
  contents: write

name: Build and Publish
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: npm ci

      - name: Read package metadata
        id: pkg
        shell: bash
        run: |
          name=$(node -p "require('./package.json').name")
          version=$(node -p "require('./package.json').version")
          echo "name=$name" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "zip_path=dist/$name-$version.zip" >> "$GITHUB_OUTPUT"

      - name: Verify packaged file exists
        run: |
          test -f "${{ steps.pkg.outputs.zip_path }}"
          ls -lh "${{ steps.pkg.outputs.zip_path }}"
      
      - name: Upload zip to release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.pkg.outputs.zip_path }}

      - name: Upload to Nexus Mods
        uses: Nexus-Mods/upload-action@v1.0.0-beta.3
        with:
          api_key: ${{ secrets.NEXUSMODS_API_KEY }}
          file_group_id: 2261332
          filename: ${{ steps.pkg.outputs.zip_path }}
          version: ${{ steps.pkg.outputs.version }}
          display_name: "Plants vs Zombies: Replanted Support v${{ steps.pkg.outputs.version }}"
          description: |
            Adds support for Plants vs Zombies: Replanted to Vortex
          file_category: main
